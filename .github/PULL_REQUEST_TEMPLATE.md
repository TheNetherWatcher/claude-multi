## What & why

## Testing

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Tested manually on: (macOS / Linux / Windows)

## Platform impact

Does this touch auth isolation (Keychain / credentials.json / Windows Credential Manager) or the symlink store? If yes, describe the behavior on each OS.
